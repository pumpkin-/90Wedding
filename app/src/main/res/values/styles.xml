<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light"></style>

    <style name="CustomThemeByron" parent="android:Theme.Holo.Light">
        <item name="android:actionBarDivider">@null</item>
        <!-- <item name="android:textColor">#000</item>
         <item name="android:actionMenuTextColor">#000</item>-->
        <item name="android:textSize">16sp</item>
    </style>

    <style name="MyActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">#ffffff</item>
        <item name="android:titleTextStyle">@style/CustomActionBarAppearance</item>
    </style>

    <style name="CustomActionBarAppearance">
        <item name="android:textColor">#000</item>
        <item name="android:actionMenuTextColor">#000</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="loadingDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">false</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 模糊 -->
    </style>

</resources>
